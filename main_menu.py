from sorting_window import sorting_window as sort_win


# TODO make a text based menu where an algorithm can be chosen

# (just making sure everything works properly with bubble sort)
sorter = sort_win()
sorter.loop("bubble")